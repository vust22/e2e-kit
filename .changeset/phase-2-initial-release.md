---
'@invertus/e2e-core': minor
'@invertus/e2e-prestashop': minor
---

First published release. Adds the CI job matrix (`e2e-kit ci-matrix`), the shared
`checkout-matrix` / `back-office-verify` / `refund` / `bo-order-management` suites, and the
`PspContract` capability flags.
